@model CarHub_Web.Models.VM.CarImagesCreateVM

<form asp-action="CreateCarImage" enctype="multipart/form-data">
    <br />
    <div class="container border p-3">
        <h4 class="text-primary">Add @Model.Car.Name Images</h4>
        <hr />

        <div class="row">
            <div asp-validation-summary="All" class="text-danger"></div>
            <input hidden asp-for="Car.Id" />
            <input hidden asp-for="CarImage.Id" />
            <input hidden asp-for="Car.Name" />
            <input hidden asp-for="Car.StartingPrice" />
            <input hidden asp-for="Car.EndPrice" />
            <input hidden asp-for="Car.ManufacturingYear" />


            <div class="form-floating py-2 col-12">
                <input type="file" name="files" class="form-control border-0 shadow" multiple />
                <label asp-for="CarImage.ImageUrl" class="ms-2"></label>
            </div>

            <div class="col-5 offset-2">
                <input type="submit" value="Create" class="btn btn-primary form-control" />
            </div>

            <div class="col-5 ">
                <a asp-action="IndexCar" asp-controller="Car" class="btn-primary btn form-control ">Back to List</a>
            </div>



            @*   <div class="container-fluid d-flex justify-content-evenly">
            @if (Model.CarImagelist != null)
            {
            foreach (var image in Model.CarImagelist)
            {
            <div class="border p-1 m-2 text-center col-12 col-lg-4">
            <img src="@image.ImageUrl" width="100%"
            style="border-radius:5px; border:1px solid #bbb9b9; height:200px; width:200px;" />
            <a asp-action="SetCarImage" class="btn btn-primary" asp-route-imageId="@image.Id" asp-route-carId="@image.CarId">
            Set As Default
            </a>
            <a asp-action="DeleteImage" class="btn btn-danger" asp-route-id="@image.Id">
            <i class="bi bi-trash-fill"></i> Delete
            </a>
            </div>
            }
            }
            </div>*@

        </div>
    </div>
</form>


@*<div class="row col-lg-3 d-flex justify-content-evenly">
    @if (Model.CarImagelist != null)
    {
        foreach (var image in Model.CarImagelist)
        {
            <div class="border p-1 m-2 col-2 text-center">
            </div>
        }
    }
</div>*@

<div class="container">
    <div class="row">
        @if (Model.CarImagelist != null)
        {
            @foreach (var image in Model.CarImagelist)
            {
                <div class="col-md-2 col-sm-4 my-2 mt-3">
                    <div class="text-center">
                        <img src="@image.ImageUrl" width="100%"
                             style="border-radius:5px; border:1px solid #bbb9b9" />

                        <div class="d-flex justify-content-evenly">
                            <a asp-action="SetCarImage" class="btn btn-primary" asp-route-imageId="@image.Id" asp-route-carId="@image.CarId">
                                Set As Default
                            </a>
                            <a asp-action="DeleteCarImage" class="btn btn-danger" asp-route-imageId="@image.Id" asp-route-carId="@image.CarId">
                                <i class="bi bi-trash-fill"></i> Delete
                            </a>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

